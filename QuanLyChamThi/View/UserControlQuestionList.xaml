<UserControl x:Class="QuanLyChamThi.View.UserControlQuestionList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLyChamThi.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DockPanel LastChildFill="True">
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                <Label Content="Danh sách câu hỏi:      " VerticalAlignment="Center"/>
                <Button x:Name="btnFilter" Content="Tuỳ chọn" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </StackPanel>

            <ScrollViewer x:Name="svGrid" VerticalScrollBarVisibility="Auto">
                <DataGrid x:Name="dgQuestionList" AutoGenerateColumns="False" Width="{Binding ElementName=svGrid, Path=ActualWidth}" CanUserResizeColumns="False" CanUserReorderColumns="False" CanUserSortColumns="False" SelectionUnit="FullRow" Loaded="dgQuestionList_Loaded">
                    <DataGrid.Columns>
                        <DataGridTextColumn x:Name="colQuestionID" Header="Mã câu hỏi" Width="2*"/>
                        <DataGridTextColumn x:Name="colSubjectName" Header="Môn thi" Width="3*"/>
                        <DataGridTextColumn x:Name="colQuestionContent" Header="Nội dung" Width="5*"/>
                    </DataGrid.Columns>

                    <DataGrid.RowStyle>
                        <Style>
                            <Setter Property="ItemsControl.Background" Value="LightPink"/>
                        </Style>
                    </DataGrid.RowStyle>
                    <!-- 
                    <DataGrid.Resources>
                        <Style x:Key="alternatingWithTrigger" TargetType="{x:Type DataGridRow}">
                            <Style.Triggers>
                                <Trigger Property="DataGridRow.AlternationIndex"  Value="2">
                                    <Setter Property="RowBackground" Value="Red"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style> 
                    </DataGrid.Resources> 
                    -->
                </DataGrid>
            </ScrollViewer>
        </DockPanel>
    </Grid>
</UserControl>
